# Dependencies
node_modules

# Next.js
.next
out

# fumadocs generated
.source

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript incremental cache
*.tsbuildinfo

# stale lockfile from pre-migration (canonical is package-lock.json)
yarn.lock

# local handoff scratchpad — internal references, not for the public repo
handoffs/
